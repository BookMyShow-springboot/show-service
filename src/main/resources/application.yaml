server:
  port: 8082


spring:
  application:
    name: show-service

  datasource:
    url: jdbc:mysql://localhost:3306/bookmyshow_show_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

